# TEC-8 实验平台数字钟（EPM7128）说明

## 功能概述
- 6 位显示：HHMMSS（时-分-秒）
- 系统时钟分频：将输入时钟分频为 1Hz 计时节拍
- 计数控制：运行/暂停
- 校时：选择字段并用脉冲加 1
- 复位：异步低有效复位

## 数码管输出方式
- LG1（第 1 个数码管）：直接输出 7 段段码，按共阴极显示（段信号高电平点亮）
- LG2~LG6（其余 5 个数码管）：输出 4 位 BCD（0~9），由实验板上的译码电路显示

本工程显示位分配如下：
- LG1：秒个位（H10）——7 段段码输出
- LG2：秒十位（H1）——BCD 输出
- LG3：分钟个位（M10）——BCD 输出
- LG4：分钟十位（M1）——BCD 输出
- LG5：小时个位（S10）——BCD 输出
- LG6：小时十位（S1）——BCD 输出

## 引脚与信号（以工程 .qsf 为准）
顶层模块名：`test_clock`

- `rst_n`：复位（低有效）
- `clk`：输入时钟（用于分频得到 1Hz）
- `sw_auto_move`：输入时钟频率选择
  - 0：假定 `clk` 为 1kHz（分频计数 1000）
  - 1：假定 `clk` 为 100Hz（分频计数 100）
- `start`：运行/暂停切换（每次上升沿翻转一次）
- `stop`：进入/退出校时模式（每次上升沿翻转一次）
- `pill_pulse`：校时加 1 脉冲（校时模式下，每次上升沿加 1）
- `alarm`：校时模式指示（约 1Hz 闪烁）

## 使用方法
### 1) 正常计时
- 上电后默认运行。
- 拨动/按下 `start` 产生一次上升沿：运行 ↔ 暂停。

### 2) 进入校时
- 拨动/按下 `stop` 产生一次上升沿：进入/退出校时模式。
- 校时模式下：计时暂停，`alarm` 以约 1Hz 闪烁作为提示。

### 3) 选择校时字段
- 校时模式下，`start` 每次上升沿切换字段：小时 → 分钟 → 秒 →（回到）小时。

### 4) 对当前字段加 1
- 校时模式下，`pill_pulse` 每次上升沿对当前字段加 1：
  - 小时范围 00~23
  - 分钟范围 00~59
  - 秒范围 00~59

## 代码与工程文件
- 顶层代码：[teat_clock.v](file:///c:/clock/teat_clock.v)
- 引脚约束：[teat_clock.qsf](file:///c:/clock/teat_clock.qsf)
